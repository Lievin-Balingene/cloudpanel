# V-zone — OpenLiteSpeed backend include (behind Nginx).
# All listeners + virtual hosts are generated by the panel into:
#   /var/lib/vzone/ols/vzone-vhosts.conf

include /var/lib/vzone/ols/vzone-vhosts.conf
