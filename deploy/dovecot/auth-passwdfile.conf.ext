passdb {
  driver = passwd-file
  # Le préfixe {SHA512-CRYPT} dans dovecot-users prime ; scheme = défaut si absent
  args = username_format=%u __MAPS_DIR__/dovecot-users
}
userdb {
  driver = passwd-file
  args = username_format=%u __MAPS_DIR__/dovecot-users
}
